## New module / update

**Module id:** `com.example.raffle`

**Action:** ⬜ New module · ⬜ Update existing module

### Checklist

- [ ] `id` is reverse-DNS and **exactly matches** the `manifest.json` `id` in my `.lumenpack`.
- [ ] `repo` (`owner/repo`) has a **GitHub Release** with `<id>-<version>.lumenpack` attached (or my binary follows the catalog's asset naming).
- [ ] `tagline` (1 line) and `description` (markdown) reflect the current version.
- [ ] Icon/cover paths exist in this repo (if referenced) and I own the rights to those assets.
- [ ] (Optional) Localized README variants exist at my repo root: `README.pt-BR.md`, etc., per the Lumen naming convention.
- [ ] `approvedAt` updated; `status: "approved"`.

### Notes for the reviewer

<!-- Anything the reviewer should verify (first release, permissions declared, links). -->

### Screenshots

<!-- Optional: paste preview images of the store row / detail page. -->